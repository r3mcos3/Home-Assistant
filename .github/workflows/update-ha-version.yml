name: Update Home Assistant Version

on:
  push:
    paths:
      - '.HA_VERSION'
    branches:
      - main

jobs:
  update-badge:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Read version from .HA_VERSION
        id: version
        run: |
          VERSION=$(cat .HA_VERSION | tr -d '\n\r')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Update README overview section
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          # Update overview section only (line 21)
          sed -i "s|This configuration runs on \*\*Home Assistant [0-9.a-z]*\*\*|This configuration runs on **Home Assistant ${VERSION}**|g" README.md

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet README.md; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push if changed
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git commit -m "🔄 Update Home Assistant version to ${{ steps.version.outputs.version }}"
          git push
